@@ -1,10 +1,10 @@
 CONFIG = {
     'working_dir': '/home/box/web/ask/',
     'python': '/usr/bin/python',
      # 'user': 'www-data',
      # 'group': 'www-data',
      'args': (
          '--bind=0.0.0.0:8000',
          '--workers=4',
          # '--worker-class=egg:gunicorn#sync',
          # '--timeout=30',
         'ask.wsgi',
     ),
 }
